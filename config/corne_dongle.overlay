/ {
    /* Remove encoder nodes completely for the dongle build */
    /delete-node/ encoder_left;
    /delete-node/ encoder_right;

    /* Replace the sensors node with an empty one */
    sensors {
        status = "disabled";
    };
};
